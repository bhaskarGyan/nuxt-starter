{
  "private": true,
  "scripts": {
    "build": "npx nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "^3.0.0-rc.12",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "nuxt-cache-ssr": "^1.0.5",
    "vite-plugin-compression": "^0.5.1"
  }
}
